## Ubuntu Initial Setup

Updated the system packages to ensure the VM was fully patched before installing services.

```bash
sudo apt update && sudo apt upgrade -y

Ubuntu-update.png

## Ubuntu Apache installed

screenshot: apache-runnng.png
